name: "node-redciaq"
services:
  script:
    build: ./app/
    container_name: script
    depends_on:
      - weewx
    networks:
      - node-inter
  weewx:
    image: instantlinux/weewx:5.1.0
    container_name: weewx
    networks:
      - node-inter
networks:
  node-inter:
    driver: bridge
